
outils:
  order: 4
  label: Outils
  type: mysql
  params:
    connexion:
      port: null
      host: localhost
      basename: portail
      username: root
      userpwd: eclipse
    search: where
    view: outils
    fields: [nom, lien, description]

#documents:
#  order: 3
#  label: Documents du groupe
#  type: files
#  params:
#    url: 'localhost/workdev/portail/documents/index.php?'
#    options: 'titlemode=?&query=?&apicall'
    
routagetel:
  order: 1
  label: Annuaire IP
  type: mysql
  params:
    connexion:
      port: null
      host: 10.110.76.246
      basename: kiplink
      username: speeddial
      userpwd: speedpass
    search: where
    view: speeddial
    fields: [number, dial_number, comment]
    labels: ['Abrégé', 'Numéro', 'Libellé']

providers:
  order: 2
  label: Fournisseurs
  type: mysql
  params:
    connexion:
      port: null
      host: localhost
      basename: portail
      username: root
      userpwd: eclipse
    search: where
    view: provider
    fields: [nom, mail, contact1]
    labels: ['Nom', 'Email', 'Contact']
    